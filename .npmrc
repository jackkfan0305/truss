# The dependency graph includes optional React Native peers through development
# tooling. Keep installs aligned with the committed lockfile instead of having
# each machine synthesize a different peer tree.
legacy-peer-deps=true
